.content(data-role="content")
  %h2 The Mixologists
  %ul(data-role="listview")
    - @users.each do |user|
      %li
        = link_to(user) do
          %strong= user.name
          .ui-li-count= "#{user.drinks.count} Drinks"
            